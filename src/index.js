import React from "react";
import ReactDOM from "react-dom";
ReactDOM.render(
  <div>
    <h1> hello world!</h1>
    <p>new</p>
  </div>,
  document.getElementById("root")
);
//with jsx we can use html in javascript code  to avoid too much java script code like document.innerhtml ...
